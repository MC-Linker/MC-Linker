version: '3.5'
services:
  mc-linker:
    image: lianecx/${SERVICE_NAME}
    container_name: ${SERVICE_NAME}
    restart: on-failure
    ports:
      - "${BOT_PORT}:${BOT_PORT}"
    volumes:
      - type: bind
        source: ./serverdata
        target: /usr/src/app/serverdata
      - type: bind
        source: ./userdata
        target: /usr/src/app/userdata
